# SangamMW Frontend

Placeholder for **Phase 5 — Visual Canvas & Designer UI**.

The implementation plan (`mw-plan.md`) schedules this app for weeks 17–24:

- React + TypeScript + Vite
- React Flow canvas (19 node types)
- Monaco Editor for SQL / Python
- Visual mapper, live preview, debugger, SSE run animation

Do not start the canvas here until Phase 4 (flow engine + SSE APIs) is in place.
The backend already allows CORS from `http://localhost:5173`.
